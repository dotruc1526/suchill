# FE-001 — Sửa TypeScript baseline

> Status: REVIEW\
> Last updated: 2026-09-24

## Assignment

- Phase / milestone: Foundation / Milestone 0 (M0-02)
- Workstream: FE-FOUNDATION
- Accountable owner: Hưng (Member 3)
- Executor type: Human + AI
- Executor name: Bảo Hưng + AI
- Reviewer: Vinh (Member 5)
- Codex task/thread: hung/sync-npm-lockfile
- Branch: hung/sync-npm-lockfile (PR #13)
- Started: 2026-09-24
- Depends on: DOC-010

## Scope

- In scope: Sửa 18 lỗi typecheck TypeScript baseline; chuẩn hóa quy trình cài đặt qua `npm ci`; đồng bộ duy nhất `package-lock.json` và loại bỏ `pnpm-lock.yaml`; thực hiện và ghi nhận đầy đủ bằng chứng smoke test giao diện qua `npm run dev` và `npm run build`.
- Out of scope: Tái cấu trúc feature logic, thêm tính năng mới, thay đổi API contract.
- Files claimed:
  - `package-lock.json`
  - `pnpm-lock.yaml` (xóa để đồng bộ)
  - `src/components/ui/ChoiceOption.tsx`
  - `src/components/roadmap/ChapterCard.tsx`
  - `src/screens/AIScreen.tsx`
  - `src/screens/ChapterScreen.tsx`
  - `src/screens/LessonCompleteScreen.tsx`
  - `src/screens/LessonScreen.tsx`
  - `src/screens/QuizScreen.tsx`
- Shared-contract consumers: Toàn bộ runtime components dùng chung domain types.

## Acceptance criteria

- [x] Sửa import type sai từ `../data` sang `../types` trong các screens và components.
- [x] Định kiểu chuỗi cho các biến màu động trong `ChoiceOption.tsx` để tương thích token `as const`.
- [x] Chạy `npx tsc --noEmit` pass hoàn toàn 0 lỗi.
- [x] Chốt package manager là `npm`, chuẩn hóa lệnh cài đặt bằng `npm ci`, xóa `pnpm-lock.yaml`.
- [x] Smoke test giao diện & runtime thực tế qua `npm run dev` và `npm run build` không lỗi.

## Verification

- Commands/checks:
  1. `npm ci` -> pass (đồng bộ 44 packages sạch sẽ từ `package-lock.json` trong 4s, 0 vulnerabilities).
  2. `npx tsc --noEmit` -> pass (0 errors).
  3. `npm run build` -> pass (1.27s, 36 modules transformed, dist artifacts generated).
  4. `npm run dev` -> pass (Vite v8.3.0 ready in 247 ms, server chạy mượt mà tại `http://localhost:8443/`).
  5. UI runtime smoke check:
     - Khung App Shell (mobile 420px, nền parchment `#F5E6D0`, texture overlay) render đúng thiết kế.
     - TopBar hiển thị đúng số liệu XP, streak, mascot.
     - BottomNav điều hướng mượt mà qua các tab Home, Ôn tập, AI, Hồ sơ mà không văng lỗi console.
     - `ChapterCard` và `ChoiceOption` hiển thị chuẩn màu token, font chữ và feedback states sau khi fix types.
- Expected result: 0 errors, build thành công, runtime và giao diện dev ổn định.

## Progress checkpoints

| Date/time | Executor | Completed | Evidence | Next action | Blocker |
|---|---|---|---|---|---|
| 2026-09-24 | Hưng + AI | Claim task, phân tích 18 lỗi thuộc 7 files | `npx tsc --noEmit` output 18 errors | Sửa lỗi trong 7 files | Không |
| 2026-09-24 | Hưng + AI | Đã sửa 7 files; chạy lại typecheck sạch 0 lỗi | `npx tsc --noEmit` exit 0, no errors | Mở PR #12 merge vào main | Không |
| 2026-09-25 | Hưng + AI | Chốt npm, xóa pnpm-lock.yaml; thực hiện smoke test build & dev UI | `npm ci` (4s), `tsc` (0 err), `build` (1.27s), `dev` ready 247ms; UI check pass | Đẩy PR #13 để Vinh nghiệm thu M0-02 | Không |

## Handoff

- Changed files:
  - `pnpm-lock.yaml` (xóa)
  - `package-lock.json` (chuẩn hóa)
  - `src/components/ui/ChoiceOption.tsx`
  - `src/components/roadmap/ChapterCard.tsx`
  - `src/screens/AIScreen.tsx`
  - `src/screens/ChapterScreen.tsx`
  - `src/screens/LessonCompleteScreen.tsx`
  - `src/screens/LessonScreen.tsx`
  - `src/screens/QuizScreen.tsx`
  - `docs/tasks/active/FE-001.md`
- Test/build result:
  - `npm ci`: pass.
  - `npx tsc --noEmit`: 0 errors.
  - `npm run build`: pass (1.27s).
  - `npm run dev`: ready in 247ms, UI check pass không runtime error.
- Environment/migration impact: Khóa chuẩn dependency qua `package-lock.json`, quy chuẩn lệnh cài đặt `npm ci`.
- Known issues/risks: Không.
- Next owner/action: Vinh (Member 5) nghiệm thu M0-02 trên PR #13 và merge vào main.
