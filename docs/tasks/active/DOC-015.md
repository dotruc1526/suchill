# DOC-015 — Gán tên nhóm và đưa tài liệu lên GitHub

> Status: IN PROGRESS\
> Last updated: 2026-09-23

## Assignment

- Phase / milestone: Team handoff sau Phase 9; M0 open
- Workstream: Documentation + integration
- Accountable owner: Codex
- Executor type: Codex
- Executor name: Codex
- Reviewer: Product owner
- Codex task/thread: current project task
- Branch: `codex/phase-9-team-handoff`
- Started: 2026-09-23
- Depends on: DOC-010 (`DONE`)

## Scope

- In scope: gán Thọ/Trúc/Hưng/Dương/Vinh vào năm vai trò và task liên quan; tạo nhánh riêng từ HEAD hiện tại, commit docs, push remote đã chỉ định và tạo PR vào `main`.
- Out of scope: tự chốt historical reviewer, chapter/pilot; sửa production code; merge PR.
- Files claimed: `docs/project/TEAM-OWNERSHIP.md`, `docs/project/TASK-BOARD.md`, `docs/project/APP-PLAN.md` (một dòng next step), các task card có placeholder tên thành viên, `docs/tasks/active/README.md`, `AGENTS.md`, `README.md`, Phase 9 roadmap/brief, Phase 2 status metadata và card này; chuẩn hóa Markdown hard breaks trong docs để diff check sạch.
- Shared-contract consumers: toàn nhóm và AI/Codex mở repo sau khi clone.

## Acceptance criteria

- [x] Tên năm người nhất quán trong bảng vai trò và task card, không gán historical reviewer khi chưa được chỉ định.
- [x] Branch riêng `codex/phase-9-team-handoff` chứa toàn bộ thay đổi docs đã chuẩn bị; không đưa `.env.local`, cache hoặc build output vào commit.
- [x] Link/card/status và diff được kiểm tra; build/typecheck baseline được báo trung thực.
- [ ] Nhánh được push và PR vào `main` được tạo, link PR gắn vào task.

## Verification

- Commands/checks: `rg` placeholder/name, Markdown link/card checks, `git diff --check`, `git status`, `pnpm build`, direct TypeScript check nếu cần.
- Expected result: PR review được với đúng phạm vi và tên nhóm.

## Progress checkpoints

| Date/time | Executor | Completed | Evidence | Next action | Blocker |
|---|---|---|---|---|---|
| 2026-09-23 | Codex | Nhận tên nhóm và remote từ Product owner | Yêu cầu hiện tại; origin trỏ `https://github.com/dotruc1526/suchill.git` | Tạo branch, cập nhật docs, kiểm tra rồi push/PR | — |
| 2026-09-23 | Codex | Nhánh riêng đã tạo, gán tên vào board/cards và stage thay đổi an toàn | 60 Markdown files: 0 local link lỗi; `git diff --cached --check` pass; `.env.local`, `.vscode`, `dist`, `node_modules` ignored; `pnpm build` pass | Commit, push và mở PR | Typecheck baseline còn 18 lỗi, xử lý ở M0 |

## Handoff

- Changed files: `AGENTS.md`, `ARCHITECTURE.md`, `README.md`, `.gitignore` và toàn bộ `docs/` theo Git; source app không đổi.
- Test/build result: 60 Markdown files không có local link lỗi; `git diff --cached --check` pass; `pnpm build` pass; typecheck baseline còn 18 lỗi đã ghi ở DOC-014.
- Environment/migration impact: không có.
- Known issues/risks: typecheck baseline còn 18 lỗi từ trước; historical reviewer/chapter chưa được gán/chọn.
- Next owner/action: Product owner review PR và gán historical reviewer.
