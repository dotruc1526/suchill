# DOC-014 — GitHub readiness và content track Member 1

> Status: REVIEW\
> Last updated: 2026-09-23

## Assignment

- Phase / milestone: Documentation / GitHub handoff; M0 open
- Workstream: Integration + Content planning
- Accountable owner: Codex
- Executor type: Codex
- Executor name: Codex
- Reviewer: Product owner
- Codex task/thread: current project task
- Branch: current working tree (`visual-novel`)
- Started: 2026-09-23
- Depends on: DOC-010 (`DONE`)

## Scope

- In scope: kiểm tra file chuẩn bị đưa lên GitHub, ignore/secrets, branch, Markdown consistency, build; tạo task nghiên cứu sơ bộ cho Member 1 không phụ thuộc milestone code.
- Out of scope: push/PR hoặc đổi Git history, xóa file tracked cũ, chọn chapter/pilot, sửa production code.
- Files claimed: `docs/project/TASK-BOARD.md`, `docs/project/TEAM-OWNERSHIP.md`, `docs/tasks/active/CONTENT-009.md`, `docs/tasks/active/README.md`, `docs/tasks/README.md`, `README.md`, card này.
- Shared-contract consumers: Product owner, Member 1 và tất cả thành viên khi clone repo.

## Acceptance criteria

- [x] File local chứa secret/build/dependency không nằm trong danh sách đưa lên Git.
- [x] Branch, build/typecheck và vấn đề trước push được ghi rõ.
- [x] Member 1 có task nghiên cứu nguồn sơ bộ `READY`, không tự chốt chapter/pilot và không mở milestone code kế tiếp.
- [x] Board/card/index và link Markdown khớp; handoff nêu việc còn cần xử lý.

## Verification

- Commands/checks: `git status`, `git check-ignore`, `git ls-files`, `pnpm build`, `pnpm exec tsc --noEmit`, `node node_modules/typescript/bin/tsc --noEmit`, Markdown link/card checks, `git diff --check`.
- Expected result: biết chính xác mức sẵn sàng để push, không lộ secret.

## Progress checkpoints

| Date/time | Executor | Completed | Evidence | Next action | Blocker |
|---|---|---|---|---|---|
| 2026-09-23 | Codex | Kiểm tra Git/ignore/build và phát hiện khoảng trống task Member 1 | `.env.local` ignored; build pass; local `visual-novel` ahead `origin/visual-novel` 5 commits và bằng `origin/main` theo refs hiện có | Tạo CONTENT-009 và hoàn tất audit | Push Git trước đó từng bị từ chối quyền ghi `.git`; chưa retry |
| 2026-09-23 | Codex | Tạo CONTENT-009 `READY`, rà 59 Markdown files và file có thể được Git đưa lên | 0 local link lỗi, 0 task card status lệch, 0 credential pattern trong tracked/untracked non-ignored files; build pass | Product owner review DOC-014; Member 1 nhận CONTENT-009 | Typecheck baseline còn 18 lỗi trong legacy/ChoiceOption; `pnpm exec tsc` không gọi được nhưng chạy trực tiếp compiler xác nhận lỗi |

## Handoff

- Changed files: task board, team ownership, task guide, active index, root README, CONTENT-009 và card này.
- Test/build result: `pnpm build` pass; `node node_modules/typescript/bin/tsc --noEmit` fail với 18 lỗi baseline tại `src/screens/*`, `ChapterCard.tsx` và `ChoiceOption.tsx`; 59 Markdown files không có local link lỗi; `git diff --check` không lỗi.
- Environment/migration impact: không có.
- Known issues/risks: typecheck chưa đạt (việc M0/FE-001); `tatus` là file tracked cũ chứa git log, không liên quan app; DOC-011/012/013/014 còn `REVIEW`; M0 code tasks chưa có card/file claim; Thọ đã được gán là Member 1 nhưng historical reviewer chưa được chỉ định. DOC-015 đã tạo branch PR riêng từ HEAD hiện tại.
- Next owner/action: Product owner review DOC-014; Member 1 nhận CONTENT-009 và claim file trước khi nghiên cứu; integration owner tạo/claim M0 cards.
